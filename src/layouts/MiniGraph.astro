---
/**
 * @layouts/MiniGraph.astro
 * ミニグラフコンポーネント
 */
import type { Locale } from '@i18n/i18n.config';
interface Props {
    locale: Locale;
    localeBaseUrl: string;
    currentPath: string;
}
const { locale, localeBaseUrl, currentPath } = Astro.props;
---